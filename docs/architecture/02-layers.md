# Layers and crate boundaries

**Status**: v0.14 (draft, 2026-09-02). ⚠️⚠️ **v0.14 RECORDS THAT THE MODEL TIER’S CENTRAL ABSTRACTION CANNOT CARRY INFERENCE, WHICH THIS DOCUMENT HAS NEVER SAID.** `fuel-model-core` now EXISTS (`96e1c5de`) and this file has been describing it as working dispatch. Measured at `4ff1fa85`: `trait Model` is **ONE METHOD**, `architecture(&self) -> &str`; `impl Model for` yields **3 types, ALL SCAFFOLDING** (`LocalModel` in `fuel-model-core/tests/registry.rs`, plus `FixtureModel` and `UmbrellaModel` in crates whose names end in `-fixture`); **ZERO production models implement it.** **`forward` is deliberately NOT on the trait** — *“forward IS the architecture”* — so `AutoModel::from_path` returns a handle you can only ask `architecture()` of, and running it requires a downcast: **the match statement the registry was meant to remove, relocated into a type.** Full treatment in the Models-layer section. Previously: v0.13 (draft, 2026-09-02). **v0.13 CORRECTS THE AXIS RULE 1 CONSTRAINS, and records that this document names a crate that no longer exists.** Previously: v0.12 (draft, 2026-09-02). **v0.12 PLACES THE `fuel` FACADE CRATE, which this document did not mention at all (measured: ZERO occurrences, against four for `fuel-core` as a control), and CORRECTS RULE 1’S ENFORCEMENT CLAIM. Both below.** Previously: v0.11 (draft, 2026-08-28). **v0.11 marks SIX more occurrences the v0.9/v0.10 passes missed — the SEVENTH iteration of one defect in this file, found by a lane applying the check mechanically rather than reading.** Two were in the diagram's INTERCHANGE row while every sibling row already carried the marker; two were the THIRD name on a line whose first two were marked; two were present-tense bullet definitions. **Four remediation passes, each correct about what it touched, none able to establish it had touched everything.**  **v0.10 marks the PROSE claims v0.9 missed — v0.9 marked the diagram, and the same document's prose went on asserting the identical names unmarked, including a BOLDED present-tense heading (*"fuel-loaders supports remote sources"*) for a crate that does not exist.** **THIRD ITERATION OF ONE DEFECT IN THIS FILE, and the progression is the argument: 2026-07-29 note goes stale → v0.8 remedies it with another prose note (too weak) → v0.9 marks the diagram (correct, and PARTIAL).** **Each remedy was stronger than the last and each was scoped to THE REPRESENTATION THAT PROMPTED IT.** **MARKING ONE REPRESENTATION OF A CLAIM DOES NOT MARK THE OTHERS — and nothing enumerated them, which is why the next remedy is a MECHANICAL GUARD rather than a fourth careful read (ROADMAP item 9).**  **v0.9 MARKS THE FOUR ABSENT NAMES INSIDE THE DIAGRAM ITSELF (†), because v0.7 and v0.8 appended NOTES and the diagram went on making the claims.** Measured at head: `fuel-format-*`, `fuel-loaders`, `fuel-tensor`, `fuel-autograd` and `PlanStore` all still read as CURRENT entries in the layer diagram, and the doc-vs-code audit correctly counted every one of them as a live present-tense claim. **THE COUNT DID NOT MOVE BECAUSE THE CLAIM DID NOT MOVE** — the disposition was recorded ADJACENT to the artifact the reader (and the extractor) actually reads. ⚠️ **AND THIS FILE HAD ALREADY DIAGNOSED THAT EXACT FAILURE: v0.8 exists because the 2026-07-29 `fuel-nn` as-built note went stale, and its own text says *"the remedy for a stale diagram entry was itself a diagram-adjacent prose claim."* I wrote that, then applied the same weak remedy to four more names in the same diagram.** **A note BESIDE a diagram does not correct the diagram; only editing the diagram does.**  **v0.8 supersedes the 2026-07-29 `fuel-nn` as-built note — the crate has SHIPPED, so the note that corrected a stale diagram entry had itself gone stale** — and extends its treatment to the three names in the same diagram position that never received it (`fuel-tensor`, `fuel-autograd`, `fuel-loaders`). **Precedent-following rather than novel: the defect class, the recorded consumer injury (a reader wrote `use fuel_nn::VarBuilder` and hit a wall, the Lightbulb port) and the remedy were all already in this file, applied to one of four names.**  **v0.7 strikes `PlanStore`, which this document listed as a CURRENT Foundation boundary in two places while the code records its DELETION** — `fuel-core/src/planner.rs` (*“was deleted with the legacy `compile_plan`/`PlanStore`”*) and `fuel-dispatch/src/optimize.rs` (*“the legacy `compile_plan`/`PlanStore` route-picking”*). **Zero definitions exist; all four `.rs` occurrences are prose, and two of them record the removal.** Found by the 2026-08-27 doc-vs-code audit (ROADMAP item 9). **It is struck rather than deleted, and joins the retirement clause the SAME SENTENCE already applies to `fuel-graph-router` and `fuel-graph-executor` — the correct treatment was two words away and this entry simply never received it.**  **v0.6 corrects the Models-tier registration promise, which `s2a2apdo` measured FALSE as worded while building `fuel-model-core` (ROADMAP item 8 increment I, `96e1c5de` / `b77b987a`) — scoped, not retracted: it holds via the `fuel-transformers` umbrella and requires one link-forcing line on the granular path. See the Models-layer section.**  **v0.5 resolves the fused-op kernel-side registry placement (`BackendImpl` / `FusedKernelRegistry` live in `fuel-dispatch`, not `fuel-memory`) and pins the `fuel-core`/`fuel-core-types` → `fuel-ir` + `fuel-hardware` retirement trajectory.** v0.5 also reflects the **two-tier runtime-extensibility** model from the 2026-06-20 adaptive-runtime-fusion decision (G4, [10-decisions-log](10-decisions-log.md)): the binding-table catalog is already Tier-1 runtime-extensible, and the fused-op metadata registry becomes Tier-2 runtime-updatable via the declarative form — so neither is a purely static Foundation commitment any longer. **v0.4 updated Foundation crate boundaries for the executor-unification + "plan IS the graph" redirection**: `fuel-storage` is renamed **`fuel-memory`** (the in-memory storage substrate only — `Storage` + `BackendStorage`); dispatch / the executor / the ranker live in **`fuel-dispatch`** (`PipelinedExecutor`), and the former `fuel-graph-router`, `fuel-graph-executor` **and `PlanStore`** are retired; the always-available correctness floor is the **CPU backend** (the Reference backend was retired, [05](05-backend-contract.md) v0.4); and load (`map_from_file`, native `.fuel`) is distinguished from import. v0.3 changes: the IO and Models layers gain explicit crate tiers for model interchange (per [13-interchange](13-interchange.md)) — a format tier (`fuel-formats` core + IR-free `fuel-format-*` leaves), an interchange tier (`fuel-interchange-weights` + `fuel-interchange-graph` cores + per-format `fuel-format-interchange-*` binding leaves), and a model tier (`fuel-model-core` registry + `fuel-model-*` leaves). v0.2 changes: fuel-loaders explicitly supports remote sources (HuggingFace Hub, GitHub, HTTPS) with sibling-cache and tolerance-recipe auto-discovery alongside the loaded model file.

How fuel is decomposed into crates, what each crate owns, and which way dependencies flow. Anchored in the existing ROADMAP layer model; this section pins the architectural intent so phase work doesn't drift.

---

## The layer model, restated

Fuel is seven conceptual layers stacked downward. Dependencies flow only downward — no lower layer may depend on a higher one. Users can stop at exactly the layer they need; each layer is independently usable.

```text
┌────────────────────────────────────────────────────────────────────────────┐
│  Use-Case Orchestration                                                    │
│  fuel-inference, fuel-training (leaf crates — nothing depends on either)   │
├────────────────────────────────────────────────────────────────────────────┤
│  Models                                                                    │
│  fuel-model-core (registry, AutoModel), fuel-model-* (one per model;        │
│  common models pre-split, long tail lazy), fuel-transformers (umbrella)     │
├────────────────────────────────────────────────────────────────────────────┤
│  Interchange                                                               │
│  fuel-interchange-weights†, fuel-interchange-graph† (cores),                │
│  fuel-format-interchange-* (per-format binding: onnx, gguf, safetensors…)  │
├────────────────────────────────────────────────────────────────────────────┤
│  IO / Formats                                                              │
│  fuel-formats (core), fuel-format-*† (one per format, IR-free parse),       │
│  fuel-loaders† (transport adapters incl. remote sources)                    │
├────────────────────────────────────────────────────────────────────────────┤
│  NN                                                                        │
│  fuel-nn (layers, losses, optimizers, parameter utilities, initialization) │
├────────────────────────────────────────────────────────────────────────────┤
│  Foundation                                                                │
│  fuel-tensor†, fuel-autograd†, fuel-graph, fuel-dispatch, fuel-memory,      │
│  fuel-core-types   (PipelinedExecutor + ranker + PlanStore† in fuel-dispatch)│
├────────────────────────────────────────────────────────────────────────────┤
│  Backends / Kernels                                                        │
│  fuel-cpu-backend, fuel-cuda-backend, fuel-vulkan-backend,                  │
│  fuel-metal-backend, fuel-aocl-cpu-backend, fuel-mkl-cpu-backend,         │
│  fuel-vulkan-kernels, fuel-conv, fuel-quantized                            │
└────────────────────────────────────────────────────────────────────────────┘

† ABSENT AT HEAD. USED THROUGHOUT THIS DOCUMENT, not only in this diagram:
  v0.9 marked the diagram and the PROSE below went on making the same claims
  unmarked -- the THIRD iteration of one defect in this file. A mark on one
  REPRESENTATION of a claim does not mark the others. The diagram states the
  TARGET layering;
  these four names have no crate/type in the workspace. See the as-built notes
  below. Marked IN THE DIAGRAM because a note BESIDE a diagram does not correct
  the diagram -- v0.7/v0.8 appended notes for exactly these names and every one
  still read as a current claim here, which is how they stayed in the audit count.
```

The Foundation layer is where most of this architecture-doc set lives. The IR (03), the optimizer (04), the runtime (06), the persistence layer (11) are all Foundation concerns. Backends (05) sit beneath. Higher layers consume the Foundation surface but don't shape it.

> ⚠️⚠️ **AS-BUILT NOTE SUPERSEDED 2026-08-27 — `fuel-nn` HAS SHIPPED, AND THIS NOTE ROTTED THE SAME WAY THE DIAGRAM IT WAS CORRECTING DID.** Measured: **`fuel-nn/` exists with 23 `.rs` files and IS a workspace member** (`grep -c '"fuel-nn"' Cargo.toml` → 1). **The remedy for a stale diagram entry was itself a diagram-adjacent prose claim, and it went stale in under two months** — which is the argument that a note is a WEAK remedy and the durable one is a GATE. **The 2026-08-27 doc-vs-code audit's extractor is that gate: it enumerates every artifact these documents name and checks existence.** Running it in CI would make this class of note unnecessary rather than periodically wrong (ROADMAP item 9).
>
> ⚠️ **AND THE NOTE WAS APPLIED TO ONE OF FOUR NAMES IN THE SAME DIAGRAM POSITION.** `fuel-tensor` and `fuel-autograd` (`:34`), `fuel-loaders` (`:28`) and `PlanStore` (`:35`) sit identically and never received it. **Measured at head: `fuel-tensor` ABSENT · `fuel-autograd` ABSENT · `fuel-loaders` ABSENT · `PlanStore` RETIRED** (struck in v0.7 — the code's own comments record its deletion). **`fuel-tensor`/`fuel-autograd` are the ratified post-fission split (Phase 7.5 work item E); the migration design defers `fuel-autograd` explicitly until a consumer demonstrates the boundary, so this diagram currently claims something the path deliberately has not built.** **`fuel-loaders` is additionally claimed in the PRESENT TENSE at `11-persistence.md:189` — *“fuel-loaders uses the existing `hf-hub` Rust crate”* — which is the stronger form of the same defect.**
>
> **The original note is retained below because its RECORDED INJURY is the reason any of this matters, and it is the only evidence in the corpus that this defect class has cost a real consumer:**
>
> > **As-built note (2026-07-29): `fuel-nn` is a planned crate, not a shipped one.** The diagram above describes the target layering. There is no `fuel-nn` directory and no mention of it in the workspace manifest; the NN surface currently lives in `fuel-core` as `lazy_nn_*` modules — `fuel_core::lazy_nn_varbuilder::VarBuilder`, `fuel_core::lazy_nn_varmap::VarMap`, plus `lazy_nn_loss` / `lazy_nn_optim` / `lazy_nn_dropout` / `lazy_nn_gru` / `lazy_nn_prelu` / `lazy_nn_one_hot` / `lazy_nn_conv_transpose`. Extracting it is a step of the `fuel-core` retirement program. Recorded because a consumer reading this diagram wrote `use fuel_nn::VarBuilder` and hit a wall (the Lightbulb port, 2026-07-29); the same stale reference appears in `fuel-inference/src/lib.rs`'s header.

## Two architectural rules that keep the layering clean

**Rule 1: dependencies flow downward only.** Enforced via Cargo's dep graph. A leaf-layer crate (fuel-inference) can depend on Models, IO, NN, Foundation, Backends. A Foundation crate cannot depend on Models or higher. Violations are caught at build time.

⚠️⚠️ **CORRECTED 2026-09-02 — "ENFORCED VIA CARGO’S DEP GRAPH" AND "CAUGHT AT BUILD TIME" ARE FALSE AS STATED, AND LIVE VIOLATIONS PROVE IT.** **Cargo enforces ACYCLICITY. It does not enforce layer DIRECTION.** A crate in a lower layer depending on one in a higher layer is legal to cargo and compiles silently. **TEN crates depend on the `fuel` facade and every one of them builds. Classified BY LAYER:**

    LEGITIMATE, at or above the facade
      fuel-examples  fuel-lazy-examples  fuel-tensor-tools   applications / CLI
      fuel-inference  fuel-training                          Use-Case Orchestration (top)
    VIOLATIONS, below the facade
      fuel-nn              NN
      fuel-transformers    Models
      fuel-onnx            Interchange (as-built form of fuel-format-interchange-onnx)
    UNCLASSIFIABLE
      fuel-parallel  fuel-datasets    NOT PLACED IN THIS DIAGRAM AT ALL

⚠️⚠️ **THE COUNT IN THE FIRST DRAFT OF THIS PARAGRAPH WAS "SEVEN" AND IT WAS WRONG, BY THE CRITERION STATED TWENTY LINES BELOW IT.** `fuel-inference` and `fuel-training` are the TOP layer, ABOVE the facade, so depending on it is CORRECT — the diagram says so on its own line. **I asserted a population without applying my own rule to it, which is the defect this document keeps recording.** ⚠️ **AND THE TWO UNCLASSIFIABLE CRATES ARE THE SAME DEFECT AS THE FACADE ITSELF: `fuel-parallel` and `fuel-datasets` appear NOWHERE in the layer model, so no layer rule can reach them either.** *(Control that this is a real classification and not a naming sweep: `fuel-tensor-tools` also depends on the facade and is NOT a violation — `cargo metadata` reports `{bin: 1}`, no `lib.rs`, zero reverse-deps, i.e. a leaf CLI, which is what a facade is FOR.)*

⚠️ **THE ONLY SHADOW A LAYERING VIOLATION CASTS THAT CARGO CAN SEE IS A CYCLE — AND THAT IS EXACTLY HOW THIS ONE WAS FOUND.** The seven sat unremarked for as long as the facade re-exported nothing they provide. **The moment restructure Stage 2 needed the facade to re-export the model zoo out of `fuel-transformers`, the latent upward edge became an actual `facade → fuel-transformers → facade` cycle and cargo refused it.** **A rule enforced only where it happens to produce a cycle is not enforced; it is occasionally noticed.** **If Rule 1 is to be enforced it needs a dep-graph test that knows the layer assignment — nothing in the toolchain knows it today.** See ROADMAP item 12.

⚠️⚠️ **AND v0.13 GOES FURTHER, BECAUSE A GATE BUILT AGAINST THIS DIAGRAM FOUND THE RULE IS NOT MERELY UNENFORCED — IT IS UNENFORCEABLE AS WORDED. MEASURED: a dependency-direction check reading the diagram as its layer assignment flags THIRTEEN upward violations, ALL of them Backends → Foundation, and SIX OF SIX backends depend on `fuel-ir`. Universal, not incidental.**

⚠️ **THE DIAGRAM'S VERTICAL AXIS IS NOT THE DEPENDENCY AXIS.** It is a CONSUMPTION and ABSTRACTION stack — *"users can stop at exactly the layer they need"*, hardware-proximity increasing downward. **Dependency runs the OTHER WAY across the Foundation/Backends boundary: a backend MUST depend on the IR to implement it, which is the backend contract working as designed.** This document already says as much and the wording of Rule 1 contradicts it: *"higher layers consume the Foundation surface but don't SHAPE it"* is a claim about AUTHORITY. **Rule 1 then attaches *"no lower layer may depend on a higher one"* to a picture whose vertical order encodes authority, not dependency.**

**CONSEQUENCE: a gate enforcing Rule 1 AS WRITTEN would allowlist thirteen universal entries on its first run — a guard that has surrendered before it starts.** **The DEPENDENCY ORDER is a separate thing and this document has never stated it. It now lives in `fuel-ir/tests/crate_dependency_tiers.txt`, which is NORMATIVE (where crates SHOULD sit) rather than descriptive (where `cargo metadata` puts them) — the distinction that makes it a gate rather than a report, since a file transcribing measured depths is self-satisfying by construction.** **That file is the source of truth for dependency direction; this diagram is the source of truth for consumption layering. THEY ARE DIFFERENT ORDERS AND BOTH ARE REAL.**

**Rule 2: Foundation is the substrate; backends are extensions.** The Foundation layer defines the architecture's commitments (the IR, the optimizer's surface, the binding-table catalog, the cost-model interface). Backends implement the contract the Foundation specifies. Adding a backend doesn't require Foundation changes; adding a primitive Op or a foundational concept may require backend updates to honor it. Note that two of these surfaces are **runtime-extensible**, not static commitments (2026-06-20, G4, [10-decisions-log](10-decisions-log.md)): the **binding-table catalog** is already Tier-1 extensible (`extend_global_bindings` appends a new implementation for an existing op identity and re-finalizes), and the **fused-op metadata registry** is the Tier-2 surface (trusted, Fuel-orchestrated, cost-gated registration of a new fused-op identity via the declarative form). What stays build-time-closed is the primitive `Op` enum and *untrusted* user-installable ops/rules ([09-non-goals](09-non-goals.md)).

This is what makes "compile fuel without CUDA" or "ship a fuel-cpu-only binary" work cleanly: backends are Cargo features; the Foundation works without any GPU backend. The always-available correctness floor is the **CPU backend** — it ships a `bit_stable_on_same_hardware` kernel for every primitive op (the Reference backend was retired in [05](05-backend-contract.md) v0.4; the CPU backend's always-built coverage commitment replaces it).

### What this document covers, and one crate it names that does not exist

⚠️ **`fuel-core-types` DOES NOT EXIST. It was renamed to `fuel-ir` by `f7e7af43` ("refactor(crate): rename fuel-core-types → fuel-ir (B0.1)"), and this document still names it EIGHT times — including the Foundation line of the diagram above and the crate-boundary definition below, NEITHER marked.** *(Measured: no `fuel-core-types` directory, no package declaring that name, zero root-manifest mentions; `fuel-ir` has all three, and holds exactly what the definition attributes — `DType`, `Shape`, `BackendCapabilities`.)*

⚠️ **The eight sites are NOT swept here, deliberately.** They are a MIXTURE: the diagram line and the crate-boundary definition are STALE (rename them), the *"as-built status"* paragraph is HISTORICAL (prose ABOUT a past state — renaming it destroys the record), and the retirement-trajectory sentence is half-true, since one of the two crates it names is already gone. **A blind sweep would corrupt the historical ones, so the rename is its own change with a disposition per site.**

**COVERAGE — AND THE ONLY HONEST METRIC IS *PLACED*, NOT *NAMED*:**

```text
  PLACED in the layer diagram                  20 of 40
  NAMED but NOT PLACED                         20 of 40
    of which named ONLY by the list below      11
    fuel-correctness-fixtures  fuel-cpu-kernels  fuel-kernel-seam
    fuel-kernel-seam-announce  fuel-kernel-seam-types  fuel-kernels-source
    fuel-kiss-ref-backend  fuel-metal-kernels  fuel-model-fixture
    fuel-model-umbrella-fixture  fuel-test-support
```

    ⚠️ **STATE THE CONSTRUCT WITH THE NUMBER, AND FOR THIS FILE THE CONSTRUCT MUST BE *PLACED*.** A crate NAMED in prose is not thereby assigned a layer, and only a PLACED crate can violate a layer rule. ⚠️⚠️ **THIS BLOCK FALSIFIED ITS OWN FIRST DRAFT, AND THE NOTE IS KEPT BECAUSE OF IT.** v0.13 first reported `named ANYWHERE 29 of 40 / named NOWHERE 11 of 40`, measured at `f9bdfb20`. Re-running the identical query after this section landed returns **40 and 0** — because the section LISTS the eleven, so they are now named. **The edit made its own measurement false, and a later reader re-running it would read `0 NOWHERE` as the coverage gap having CLOSED. It has not: those eleven are NAMED IN A LIST, NOT PLACED IN A LAYER.** **A count whose subject a document can change by mentioning it is not a coverage metric — PLACED is, and PLACED is unmoved at 20 of 40.** *(Control that the diagram is not simply a superset: it names SIX things that are not workspace members — five carry the † ABSENT-AT-HEAD mark, and `fuel-core-types` does not.)*

### The `fuel` facade crate (added to this document 2026-09-02)

⚠️ **`fuel` IS NOT IN THE DIAGRAM ABOVE AND WAS NAMED NOWHERE IN THIS DOCUMENT BEFORE v0.12.** It became a real crate on 2026-09-02 (`a2027651`, restructure Stage 1); this document was last revised 2026-08-28, so the omission is a DATE GAP rather than a decision. **The consequence was not cosmetic: a crate with no layer cannot violate a rule about layers, which is why seven upward dependencies onto it were never flagged.**

**`fuel` is the CONSUMER ENTRY POINT.** It re-exports the public surface so a user writes one `cargo add` and one `use`. **It is a re-export shell, not a home for logic:** it owns no types, no traits, and no behaviour beyond forwarding.

**Its layer position MOVES, and that is the part to understand:**

- **Today** it re-exports `fuel-core` only (`pub use fuel_core::*` plus an explicit `bail`, the one macro a glob does not carry), so its effective position is `fuel-core`’s — which itself still spans Foundation and Models, because it has not been dissolved yet.
- **After Stage 2** it also re-exports the model zoo, which places it unambiguously **ABOVE Models**. **At that point every library crate depending on it is formally upward and Rule 1 applies without argument.**

⚠️ **RULE: a crate may depend on `fuel` IF AND ONLY IF IT SITS AT OR ABOVE THE FACADE — an application, an example, a CLI, or a Use-Case Orchestration crate.**

⚠️ **DO NOT USE "a leaf that nothing depends on" AS THE TEST. That was this rule's first wording and it is a PROXY that misclassifies:** it would exempt any low-layer crate that merely happens to have no dependents today (`fuel-parallel` has none), and it would condemn a legitimately-placed one the moment something depends on it. **A proxy that agrees with the criterion on today's data is not the criterion. Classify by LAYER.**
 **A library crate depends on the real crate it needs (`fuel-core`, `fuel-graph`, …), never on the facade.** *(The pattern already exists in-tree: `fuel-vulkan-backend` depends on `fuel-core` directly.)* **The seven exceptions are recorded with measured repoint costs in ROADMAP item 12; `fuel-transformers` is fixed by Stage 2 and the other six are sequenced behind it.**

## Crate boundaries inside Foundation

Foundation has more crates than the diagram shows; the boundaries inside it are load-bearing. The relevant ones for this architecture doc set:

- **fuel-core-types**: dtypes, shapes, layouts, errors, the dispatch-key types, the `BackendCapabilities` shape. Zero backend dependencies. Re-exported through fuel-core for ergonomics.
- **fuel-graph**: the `Op` enum (primitive variants + `Op::Fused` arm), `Node`, `Graph`, `FusedOpRegistry` metadata types, the OptimizationMap rules, the rule engine. *(**OptimizationMap** is deliberately UNBACKTICKED: it is this corpus's ARCHITECTURAL CONCEPT — defined by its own section at [04-optimization](04-optimization.md#optimizationmap) and paired with **DecompositionMap** — not a Rust type. It was backticked here, in a list where every other member IS a real type, which made it read as a type claim and put it on the doc-vs-code audit's absence list. `RuleRegistry` in `fuel-graph` may be one REALIZATION of it; it is not the concept, and renaming the concept to it would replace established vocabulary used across ten documents with an implementation detail.)* Depends on fuel-core-types only. The `FusedOpRegistry` metadata / `OptimizationMap` is **not** frozen-at-startup-only: per the 2026-06-20 G4 decision it becomes **Tier-2 runtime-updatable** via the declarative form (append-only, stable never-reused `FusedOpId`s; pattern + recipe + cost carried as data, run by generic interpreter fns). Implementing the stubbed declarative pattern engine (`PatternKind::Declarative` at `fuel-graph/src/opt.rs`) is the prerequisite — see [04-optimization](04-optimization.md) and [10-decisions-log](10-decisions-log.md).
- **fuel-memory** (renamed from `fuel-storage`): the in-memory storage substrate only — the `Storage` struct, the `BackendStorage` enum, and allocators. No dispatch, no binding table, no kernel registry (those moved to fuel-dispatch). Depends on fuel-core-types + per-backend crates. The fused-op *kernel-side* registry (`BackendImpl` payloads / `FusedKernelRegistry`, joined to fuel-graph's metadata by `FusedOpId` — see [03-ir](03-ir.md)) lives in **fuel-dispatch**, not here: it is closer to "dispatch payload" than "storage substrate," so it sits alongside the binding-table catalog it serves. (The 2026-06-14 open question over this placement is resolved in that direction.)
- **fuel-dispatch**: the binding-table catalog, `KernelRef` ABI, `OpParams`, dispatch wrappers, the ranker/selector chain (Picker 1 cost ranking + Picker 2 runtime selection) and the **`PipelinedExecutor`** — the single executor on every realize entry (the work-item producer + the executor loop). Where 04-optimization's ranking and 06-runtime's dispatch commitments live in code. (Supersedes the retired `fuel-graph-router` and `fuel-graph-executor`.)
- **fuel-tensor†** + **fuel-autograd†** (post-fission per Phase 7.5 work item E): the user-facing handle + autograd story. fuel-tensor† wraps fuel-graph; fuel-autograd† does graph-rewrite-as-backward.
- **fuel-conv**, **fuel-quantized**: ops that warrant their own crates because they have substantial standalone value (conv has its own kernel ecosystem; quantization has its own dtype family). Both are Foundation-layer despite being "ops" because they define types that Foundation-layer consumers use.

The fission decisions in Phase 7.5 (work item E) are about cleaving consumer boundaries: Lightbulb (inference-only consumer) wants fuel-tensor† without fuel-autograd†; mlmf (network IPC consumer) wants fuel-formats without fuel-loaders†. Each split is justified by a class of consumer that uses one side and not the other.

**fuel-loaders† IS TO SUPPORT remote sources — INTENDED SURFACE, NOT AS-BUILT.** In addition to local-filesystem model loading, fuel-loaders† is to support common remote sources via URI schemes: `hf://owner/repo` (HuggingFace Hub via the `hf-hub` Rust crate), `github://owner/repo/path` (GitHub via raw.githubusercontent.com), `https://...` (any HTTPS-accessible model file). When loading from a remote source, the loader auto-discovers sibling cache files and tolerance recipes at the same location and uses any that match the user's environment fingerprint (per [11-persistence §Cache generation and distribution](11-persistence.md#cache-generation-and-distribution)). On miss, the loader downloads only the model file; fuel falls back to local optimization. The remote-source layer is leaf functionality — it depends on fuel-formats for parsing what it downloaded; it doesn't shape Foundation-layer concerns above it.

**Retirement trajectory (PARTIALLY LANDED — status corrected 2026-07-29).** `fuel-core` and `fuel-core-types` are slated to be retired before crates.io publish, because both names collide with existing crates.

> **As-built status (2026-07-29).** This paragraph previously read "intended, not yet landed" and said "until that refactor lands the old names remain authoritative." **That is no longer true and was misleading.** Verified against the workspace: **`fuel-core-types` no longer exists** — it is now **`fuel-ir`**; **`fuel-hardware`**, **`fuel-memory`** (ex-`fuel-storage`), and **`fuel-backend-contract`** all exist. What has *not* landed: **`fuel-core` itself still exists** and has not dissolved into the crates it re-exports. So every `fuel-core-types` reference in this section is **historical** — read it as `fuel-ir`. Recorded because a consumer-facing survey found this section describing crates by names the workspace no longer has. The split is along the line their contents already imply: the vocabulary (`fuel-core-types` — dtypes, shapes, layouts, errors, dispatch keys, the `BackendCapabilities` shape) becomes **`fuel-ir`**; hardware discovery (the `SystemTopology` substrate-and-transfer model, today in `fuel-core`) splits into a discovery half that becomes **`fuel-hardware`** and a dispatch-overlay half (binding-op coverage, capability cross-checks) that stays in **fuel-dispatch**; `fuel-core` itself, an umbrella facade, dissolves into the crates it re-exports. Until that refactor lands the old names remain authoritative; this note pins the destination so new load-bearing types are not added to the retiring crates.

## Crate boundaries at the IO and Interchange layers

Model import/export ([13-interchange](13-interchange.md)) splits along the weight-vs-graph axis into three tiers. Each tier is a *core + leaves* family, mirroring `fuel-formats` : `fuel-format-*`†.

**Format tier (IR-free byte parsing).** `fuel-formats` holds the shared substrate (the `Read`/`Seek` transport traits, dtype mapping, the common error type). Each format is a leaf — `fuel-format-safetensors`†, `fuel-format-gguf`†, `fuel-format-pickle`†, `fuel-format-onnx`†, … — that parses bytes into format-native structs. **No leaf depends on `fuel-graph` or any storage type**; the tier knows only `fuel-core-types`. This is what lets a safetensors-only consumer pull almost nothing. `fuel-format-onnx`† is the parse half of the former `fuel-onnx` placeholder; the map half moves up to the interchange tier.

**Interchange tier (translate ↔ the IR).** Two cores:

- `fuel-interchange-weights`† — named tensors + dtype + **quantization recipe** → fuel `Storage`. Depended on by *every* interchange leaf, weight-only and graph alike (a graph format still loads its weights). The quant interpreter lives here once.
- `fuel-interchange-graph`† — operator DAG ↔ the base map ([03-ir](03-ir.md)): the op-map helpers, the decomposition/fusion-recognition library, the conformance-matrix machinery, and the native-format read/write (which reuses [11-persistence](11-persistence.md)'s base-map serialization — no new DAG format).

Each `fuel-format-interchange-*` leaf depends on its `fuel-format-*`† peer plus the relevant core(s), and owns the one thing that can't be hoisted: the **per-format node↔weight binding** (ONNX by name, `.pt2` by blob index, TFLite by buffer index). A weight-only leaf needs only the weights core; a graph leaf needs both.

## Crate boundaries at the Models layer

The model tier is `fuel-model-core` + `fuel-model-*` leaves, with `fuel-transformers` retained as an optional umbrella.

- `fuel-model-core` — the `Model` trait, the `model_type`/`general.architecture` → builder **registry**, and `AutoModel::from_path`. ⚠️⚠️ **v0.14 — WHAT THIS BULLET PROMISES AND WHAT THE TRAIT DELIVERS ARE DIFFERENT THINGS, AND A READER BUILDING ON IT WILL HIT A WALL. THIS FILE ALREADY RECORDS THAT EXACT INJURY ONCE** (the stale `fuel-nn` diagram entry, which sent a Lightbulb porter to `use fuel_nn::VarBuilder` and a wall) — **this is the same class, pointed at a crate that DOES exist.** **MEASURED AT `4ff1fa85`, constructs named:** `trait Model` definitions **1**; its method count **1** (`architecture(&self) -> &str`); `impl Model for` **3 TYPES**, and they are scaffolding by measurement rather than characterisation — `LocalModel` (`fuel-model-core/tests/registry.rs:43`), `FixtureModel` (`fuel-model-fixture/src/lib.rs:26`), `UmbrellaModel` (`fuel-model-umbrella-fixture/src/lib.rs:29`): **one in a `tests/` directory and two in crates whose names END IN `-fixture`. Zero production models.** ⚠️ **`Box<dyn Model>` IS INFERENCE-DEAD, AND THAT IS A DESIGN DECISION RATHER THAN AN OMISSION.** `forward` lives on each concrete model because *“forward IS the architecture”*, and model inputs genuinely vary across causal-LM / encoder / vision / multimodal. **Universal-and-capable is therefore NOT AVAILABLE at this trait — a CONSTRAINT, not a gap, and it must not be “fixed” by widening `Model`.** ⚠️⚠️ **AND THE COMPARISON THAT SHOULD GOVERN ANY FUTURE WORK HERE: FUEL HAS TWO MODEL TRAITS WITH OPPOSITE DEFECTS.** `fuel-model-core::Model` is **UNIVERSAL AND EMPTY** (1 method, 3 fixture impls). **`fuel-inference::multi_session::DecodeModel` is CAPABLE AND NARROW** — `n_layers`, `layer_state_specs`, **`forward_with_kv_context_persistent`**, `supports_batched_decode`, `build_batched_decode_logits` — with **5 implementors, against 434 `pub struct *Weights` definitions in `fuel-transformers/src/models` (439 tree-wide) — i.e. the trait that can actually RUN a model is implemented for roughly one model in eighty.** **Neither is the model-loading seam on its own, and the registry is a CLASSIFICATION layer until something it hands back can run.** **CONSEQUENCE FOR THE TIER, ruled 2026-09-02 (ROADMAP item 8): the load-bearing work is NOT wiring the zoo to the registry.** It is **config-from-path** — a capability of the CONFIG TYPES, proven on ~30 models and absent from ~116, whose value does not depend on `Model` existing — and **`WeightSource`** behind it. **A builder that compiles, ignores the path it was handed and returns a hardcoded preset is the failure this tier actually has** (measured on Qwen2: `Qwen2Config` derives `Debug, Clone, PartialEq`, has no `Deserialize` and no parser, so a `build(&ModelSpec)` is forced to `qwen2_7b()` — **green, and wrong for every checkpoint that is not 7B**). Registration is **link-time distributed** (`inventory`/`linkme`). ⚠️ **CORRECTED v0.6 (2026-08-27) — THE ORIGINAL WORDING WAS MEASURED FALSE, AND THE CORRECTION IS A SCOPING ONE RATHER THAN A RETRACTION.** It read: *“merely depending on a `fuel-model-*` crate makes it appear in the registry, so ‘no feature gates’ works.”* **Rust does not link an rlib whose symbols nothing references, and `inventory`'s registration section goes with it — no compile error, no panic, the model is simply absent.** Measured at `b77b987a` (rustc 1.98.0 / `inventory` 0.3 / Windows MSVC) with three integration tests differing by one line, `extern crate` behaving identically:

| consumer reaches the leaf… | leaf in registry? |
|---|---|
| **directly**, with no reference to it | **ABSENT** |
| **through `fuel-transformers`**, whose `pub use` re-exports it | **PRESENT** |
| not at all — nothing referenced *(the discriminating control)* | registry `[]` |

**So the promise HOLDS on the batteries-included path and not on the granular one: a `pub use` IS a reference, and referencing the umbrella drags the leaf's registration in with it.** **The true rule is “depend, PLUS one link-forcing line (`use fuel_model_x as _;`) — unless you reach the leaf through the umbrella, which supplies the reference for you.”** **The scaffolder cannot fix the granular case: the line goes in the CONSUMER, so a tool emitting `fuel-model-*` crates can never emit it.** That is why the umbrella, not codegen, is the answer. **The granular failure is silent but TOTAL — registry `[]`, never partially populated — so `AutoModel` reports no architectures at all and fails at the first `from_path` rather than resolving some models and silently missing others.** A papercut, not a hazard. **Recorded in-tree as an ASSERTED ABSENCE, not `#[ignore]`d**, so the test goes red the day the behaviour changes (a newer `inventory`, a different linker, `-C link-dead-code`) — **and that red is good news whose correct response is to restore this paragraph's original wording, NOT to add a reference to the test.** What survives unchanged: **one additive line per consumer is still materially better than editing a central dispatch file**, which is the property this design was reaching for. What does not survive: **the zero-consumer-code form on the granular path, which `AutoModel` cannot promise.** The imported-graph → known-architecture *recognizer* ([13-interchange](13-interchange.md)) lives here, since it depends on both the registry and the interchange tier. ⚠️⚠️ **v0.14 — *LIVES HERE* IS A PLACEMENT CLAIM READ AS AN EXISTENCE CLAIM, AND THE RECOGNIZER DOES NOT EXIST.** Measured at `4ff1fa85`: **zero functions map an imported graph to an architecture name** — no `recogni*`, `identify_arch`, `detect_arch`, `infer_arch` or `arch_from_graph` definition anywhere in `*.rs`. *(The single `fn ...recognized...` hit is an unrelated sk4 dtype-spelling test.)* ⚠️ **POSITIVE CONTROLS, because this is an absence claim: BOTH INGREDIENTS EXIST and the query would find anything joining them** — graph pattern-matching is defined in 2 files (`fn pattern*`, the fusion path) and the `inventory` architecture registry in 4. **So the absence is of the JOIN, not of the query.** ⚠️⚠️ **AND THE NEAR-MISS IS NAMED HERE DELIBERATELY, BECAUSE IT IS WHAT A LATER READER WILL FIND AND MISTAKE FOR A REFUTATION: `fuel-core/src/quantized/arch.rs:94` DOES define `pub fn detect_from_gguf(content: &Content) -> Architecture`.** **It is not the recognizer and it cannot be.** **It READS A DECLARED LABEL** — GGUF’s `general.architecture` metadata string — **whereas the claim above is that an ARBITRARY IMPORTED DAG can be MATCHED STRUCTURALLY against the registered architectures (*"this is a Llama"*) with no such label present.** **Reading a name someone wrote into a header and inferring a name from topology are different problems; only the second needs the registry, and only the second is what an ONNX file arrives without.** ⚠️ **SECOND MEASUREMENT, and it is the more damning one: `AutoModel::` is consumed in exactly ONE file at head — `fuel-model-core/tests/registry.rs` — plus two mentions in `fuel-model-core`’s own module doc. ZERO PRODUCTION CONSUMERS.** **So the tier is not merely under-implemented, it is UNCALLED: nothing outside its own tests has ever asked it for a model.** **[13-interchange](13-interchange.md) carries the same claim in stronger form** — *"an imported graph CAN additionally be pattern-matched against the registered architectures and swapped for the optimized parametric version"* — **where `can` is capability language for something with no implementation.** **CORRECTED IN THE SAME CHANGE: [13-interchange](13-interchange.md) is bumped to v0.5 and the sentence is marked there too.** ⚠️ **An earlier draft of this note said that file "needs its own version pass" — which would have shipped a KNOWN-FALSE claim in one constitution document while recording the finding in another. A note about a defect is not a fix for it, and "someone should pass over that file" is the shape that never gets done.** ⚠️ **AND THE RECOGNIZER COULD NOT WORK EVEN IF WRITTEN, WHICH IS THE PART A LATER READER NEEDS: it matches against THE REGISTERED ARCHITECTURES, and the registry holds THREE FIXTURE MODELS AND ZERO PRODUCTION ONES** (measured above). **A recognizer over an empty registry recognises nothing** — so this is not a missing function, it is a capability whose precondition is the same unbuilt thing the rest of this section documents. **This file has shipped this exact defect class at least four times** (`fuel-loaders`†, `fuel-nn`, `fuel-tensor`†/`fuel-autograd`†, `PlanStore`), **and each previous instance was a crate NAME in the diagram. This one is a CAPABILITY SENTENCE in prose, which no diagram marker and no † convention would ever have caught.**
- `fuel-model-*` — one architecture per crate. Generic building blocks (RoPE, RMSNorm, GQA attention, SwiGLU MLP) stay in `fuel-nn`, not in a per-family crate; a shared-component crate is extracted only on *real* duplication across ≥2 model crates, never as a speculative family taxonomy (multimodal models span "families," so families aren't a partition).
- `fuel-transformers` — an optional umbrella re-exporting the `fuel-model-*` crates behind features, so a consumer gets *either* granular (`cargo add fuel-model-llama`†) *or* batteries-included, and nobody is forced into feature assembly.

**Applying the stopping rule (below) to this tier.** Per-format leaves and the high-demand `fuel-model-*` crates (Llama, Qwen, Mistral, Gemma, Phi, Mamba, Whisper, BERT, CLIP/SigLIP, Stable Diffusion/Flux) are split **now**: their single-format / single-model consumers are near-certain, so the split lands under real pressure. The long tail of architectures stays in the umbrella and is **extracted lazily** — when a real single-model consumer appears, or when the scaffolder emits a new model as its own crate. Big-banging all ~65 existing models into separate crates up front would be the speculative split the stopping rule rejects.

## Crate boundaries at the Backend layer

Each backend is its own crate, each gated by a Cargo feature on whichever consumer wants it. The pattern:

- **Backend crate** (e.g., `fuel-cuda-backend`): typed kernels operating on the backend's concrete storage type. No dependency on fuel-dispatch (the crate that dispatches *into* it).
- **Backend-side wrapper module in fuel-dispatch** (e.g., `fuel_dispatch::dispatch::cuda`): dispatch wrappers that pattern-match `BackendStorage::Cuda(...)`, extract the typed storage, and call the backend's typed kernel. fuel-dispatch depends on the backend crate; the backend doesn't depend on fuel-dispatch. Cycle avoided. *(Before the executor-unification rename these wrappers lived in `fuel-storage`/`fuel_storage::dispatch::*`; the storage substrate is now `fuel-memory` and carries no dispatch.)*
- **Hardware FFI wrappers** (e.g., `baracuda` for CUDA, `vulkane` for Vulkan, `aocl-blas-rs` for AOCL): live outside fuel entirely. Backend crates depend on these via crates.io. Fuel itself never names raw FFI.

This is how fuel can support CUDA, Vulkan, Metal, AOCL, MKL as independent compile-time-optional backends without coupling them to each other or to the Foundation layer's identity.

## What's not in the layer model

Three concerns that span layers rather than fitting into one:

- **The persisted `.fuel` graph and tolerance-recipe artifacts ([11-persistence](11-persistence.md))**: produced by Foundation, consumed by Foundation, but their format is a Foundation/Backend concern (the optimized paths embed backend kernel hashes + hardware fingerprints). Cross-cutting; the persistence section treats them holistically.
- **Empirical Judge profile data**: produced by Foundation (the Judge), consumed by Foundation (the optimizer + route picker), measured against Backends. Cross-cutting.
- **Pattern-harvest telemetry ([08-pattern-harvest](08-pattern-harvest.md))**: produced by Foundation (the optimizer reads the base map for harvest); consumed by the project's server (outside the layer model entirely).

These don't break the layer model — they're consistent with it. They just don't fit cleanly inside any one layer.

## Stopping rule for new crates

A new crate is justified only when there is a class of consumer that uses one side and not the other. Indicators:

- The consumer needs the included surface and not the excluded surface.
- The fission unlocks compile-time leanness, IPC scenarios, or independent ecosystem development.
- The split is in the dependency graph, not just in the file layout.

A speculative split (e.g., "let's separate this just in case someone wants it standalone") is rejected. Every Foundation-layer crate that exists today emerged from a real consumer pressure; new ones land the same way.

---

## See also

- [03-ir](03-ir.md) — the IR types live in fuel-graph (Foundation layer).
- [05-backend-contract](05-backend-contract.md) — what backends advertise to the Foundation layer.
- [11-persistence](11-persistence.md) — cross-cutting artifact concerns.
- [13-interchange](13-interchange.md) — the import/export model the IO / interchange / model tiers implement.
- ROADMAP §"Layer Model" — the original layer-model diagram and dependency-direction commitment.
- ROADMAP §"Phase 7.5 — Core simplification" — work item E (fission) decisions.
