# fuel-cuda-kernels

This crate contains the CUDA kernels for the fuel ML framework. It is built
only when fuel is compiled with CUDA support. Some of these implementations
come from the [dfdx crate](https://github.com/coreylowman/dfdx).
