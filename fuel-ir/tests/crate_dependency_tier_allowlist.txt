# Dependency edges that point UPWARD in `crate_dependency_tiers.txt`, with the
# REASON each is tolerated.
#
# Format:  <from>\t<to>\t<reason>
# The KEY is the first two fields, so editing a reason never breaks an entry.
# An entry with a BLANK reason FAILS the gate: a bare allowlist is amnesty with
# no record of which disposition it is, and it takes the guard's signal with it.
# This list may only SHRINK -- an entry whose edge no longer exists is STALE and
# fails, so it cannot rot into permanent amnesty.
#
# ALL THREE ENTRIES BELOW WERE FOUND, NOT PLANTED. They are the facade
# inversion: library crates depending on the consumer-facing facade `fuel`
# instead of on `fuel-core`. ROADMAP item 12 tracks undoing it; each removal
# here is one repoint landing.

fuel-nn	fuel	FACADE INVERSION (ROADMAP item 12). REASON REWRITTEN 2026-09-02 after Stage 2 (131e8b84) FALSIFIED ITS PREDICTION. It previously read "1 model reference blocking the repoint UNTIL STAGE 2 MOVES IT" and named fuel-training AND fuel-transformers as dependents. Both halves are now false. Measured at 131e8b84: only fuel-training depends on fuel-nn (fuel-transformers: 0; control: fuel-graph IS found in its deps, so the zero is absence). And the single model reference is fuel::lazy_latent_cache::LatentCache in fuel-nn/src/modules/two_proj_attention.rs -- which is EXACTLY the one file Stage 2 deliberately carved OUT of the 147 (stay-list = {lazy_latent_cache}, 147-1=146). So the event the old reason waited on will never happen: that symbol stays in fuel-core by design. The repoint is therefore NOT blocked and never needed Stage 2 -- the symbol is reachable as fuel_core::lazy_latent_cache::LatentCache today. 219 fuel:: occurrences in fuel-nn/src (construct: grep -c of the literal fuel:: across *.rs; the old 215 counted something unstated and is not re-derived here rather than assumed comparable).
fuel-onnx	fuel	FACADE INVERSION (ROADMAP item 12). Eligible to repoint today -- zero model references. Terminates in a consumer (only fuel-examples depends on it), so it is the TIDINESS tier: safe now, not urgent.
