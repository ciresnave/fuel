# SPDX-License-Identifier: MIT OR Apache-2.0
[package]
name = "fuel-model-fixture"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "Test fixture: a fuel-model-* leaf that registers itself and is NEVER referenced by name."
publish = false

[dependencies]
fuel-model-core = { path = "../fuel-model-core" }
inventory = "0.3"
