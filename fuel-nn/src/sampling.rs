//! Gumbel-softmax sampling — re-exported from `fuel_core::sampling`.
//!
//! This module is a compatibility shim. The canonical source is
//! `fuel_core::sampling`. All items are re-exported here unchanged so that
//! existing code using `fuel_nn::sampling::*` continues to compile without
//! modification.
pub use fuel::sampling::*;
