# flux
Module: lazy_flux
Lib decl: pub mod lazy_flux;
Anchor: pub mod lazy_fastvit;
Tests: 6 passed
Names: flux_dit_forward_shape_and_finite_tiny, qk_norm_inside_attention_changes_output, flux_vae_round_trip_tiny, flow_match_scheduler_step_finite, generate_end_to_end_tiny, quantized_flux_model_q4_0_close_to_source

Full Flux port — DiT (FluxModel with FluxConfig::dev()/schnell() factory configs, DoubleStream + SingleStream blocks, QK-Norm via rms_norm_affine inside attention, N-dim RoPE via per-axis rope_axis), 16-channel 8x-downsample VAE (FluxVae with FluxVaeConfig::dev(), encode/decode using GroupNorm + ResNet + spatial attention + asymmetric-padded stride-2 downsample / nearest-2x upsample), FlowMatchScheduler (linear + shifted sigmoid-tilt) + generate() driver, and QuantizedFluxModel::from_f32_bake() (Q4_0 weights for Linears with in_features % 32 == 0; smaller Linears stay F32, mirroring upstream convention). Single file lazy_flux.rs at fuel-core/src/lazy_flux.rs (1645 LOC). lib.rs declaration inserted between lazy_fastvit and lazy_gemma. Tiny test config uses hidden_size=16, num_heads=2, head_dim=8, axes_dim=[2,2,4] so each per-axis dim is even. The qk_norm_inside_attention_changes_output test compares a model with cfg.qk_norm=true vs cfg.qk_norm=false (identical weights) and asserts the outputs differ — confirms the RMSNorm code path is reached. flux_vae_round_trip_tiny exercises a (1,3,64,64) image through encode (→ 8x8 latent of 4 z-channels) + decode (→ (1,3,64,64)). 6/6 tests pass on Windows CPU.