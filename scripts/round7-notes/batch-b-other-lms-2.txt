# batch-b-other-lms-2
success: true
migrated: 
skipped: marian-mt (infrastructure gap: lazy_marian::MarianModel ships forward/forward_encoder/forward_decoder but NO safetensors weight loader (no from_hub, no load_from_mmapped, no MarianWeights::load_from_mmapped). The eager VarBuilder loads ~200 tensors by name into encoder+decoder; struct-by-struct construction would require writing that mapping inside main.rs, which exceeds the migration's stated scope (instructions: 'use the lazy module's weight loader'). All in-tree consumers use random tiny_weights() helpers from tests. Migration unlocked when MarianWeights::load_from_mmapped lands in fuel-core.)
  - mixtral (infrastructure gap: lazy_mixtral ships MixtralConfig::mixtral_8x7b_v01 + MixtralModel::forward but NO safetensors loader (no from_hub, no MixtralWeights::load_from_mmapped). Eager VarBuilder loads expert MoE weights by indexed names; no consumer-side helper exists. Migration unlocked when MixtralWeights::load_from_mmapped ships.)
  - moondream (infrastructure gap: lazy_moondream ships forward over text + vision but NO safetensors loader (no from_hub, no MoondreamWeights::load_from_mmapped). Eager binary loads both the vision encoder and Phi text model from a single safetensors file; that mapping does not exist in lazy yet.)
  - nomic-bert (infrastructure gap: lazy_nomic_bert ships NomicBertConfig::nomic_embed_text_v1_5 + forward but NO safetensors loader (no from_hub, no NomicBertWeights::load_from_mmapped). lazy_bert::BertModel::from_hub exists but is a different architecture (no rotary embed, no gated MLP). Migration unlocked when NomicBertWeights::load_from_mmapped ships.)
  - nvembed_v2 (infrastructure gap: lazy_nvembed_v2 ships NvEmbedConfig::nv_embed_v2 + forward(tokens, attention_mask) but NO safetensors loader. Eager binary uses VarBuilder over sharded shards with a Mistral encoder + latent attention pooler; no consumer-side struct loader for NvEmbedV2Weights.)
  - qwen (infrastructure gap: lazy_qwen2 / lazy_qwen2_moe / lazy_qwen3 / lazy_qwen3_moe all ship forward but NONE has from_hub or {Qwen2,Qwen2Moe,Qwen3,Qwen3Moe}Weights::load_from_mmapped. The eager binary loads Qwen2/Qwen2MoE/Qwen3/Qwen3MoE selectively from a CLI flag; without a loader on any variant, the migration cannot construct any of the four model variants from real Hub weights.)
  - recurrent-gemma (infrastructure gap: lazy_recurrent_gemma ships forward + recurrent (RGLRU) + attention block helpers but NO safetensors loader. RecurrentGemmaWeights has no load_from_mmapped sibling; eager binary uses VarBuilder over ~100 named tensors covering both temporal block types.)
  - replit-code (infrastructure gap: replit-code uses fuel_transformers::models::mpt + quantized_mpt. lazy_mpt ships MptConfig + MptModel + forward (ALiBi) but NO safetensors loader (no from_hub, no MptWeights::load_from_mmapped) and no GGUF quantized path. Migration unlocked when MptWeights::load_from_mmapped + quantized_mpt lazy shipping land.)
  - rwkv (infrastructure gap: lazy_rwkv5 / lazy_rwkv6 / lazy_rwkv7 ship forward + token-mix/channel-mix scans but NONE has from_hub or {Rwkv5,Rwkv6,Rwkv7}Weights::load_from_mmapped. Eager binary picks one of 3 RWKV variants by CLI flag; without a real-Hub loader on at least one variant the binary cannot run end-to-end.)
  - splade (infrastructure gap: splade uses BertForMaskedLM (encoder + MLM head). lazy_bert ships BertModel::from_hub returning encoder hidden states ONLY — there is no BertForMaskedLM::from_hub and no MLM-head wrapper in lazy. SPLADE's relevance vector requires the MLM logits over the full vocab; without the LM head the eager output bit-pattern cannot be preserved. Migration unlocked when lazy_bert grows BertForMaskedLM (cls.predictions: dense + layer_norm + decoder=embedding_tied + bias).)

All 10 binaries in this batch were blocked by the same root cause: the lazy modules they target (lazy_marian, lazy_mixtral, lazy_moondream, lazy_nomic_bert, lazy_nvembed_v2, lazy_qwen2/qwen2_moe/qwen3/qwen3_moe, lazy_recurrent_gemma, lazy_mpt, lazy_rwkv5/rwkv6/rwkv7, lazy_bert::BertForMaskedLM) ship the model forward graph but have NOT shipped a safetensors-from-Hub weight loader (no `from_hub`, no `*Weights::load_from_mmapped`). The lazy modules that DO ship a loader today and are referenced by other binaries: lazy_bert::BertModel, lazy_llama2c::Llama2cModel, lazy_phi::PhiModel, lazy_resnet::ResnetModel, lazy_convnext::ConvNextModel, lazy_gemma2::Gemma2Model, lazy_sd_text_encoder, lazy_sd_unet, lazy_sd_vae, lazy_whisper::WhisperModel. The pattern is consistent: in-tree consumers (tests + fuel-lazy-examples bins) either use `tiny_weights()` random helpers or the few modules with `from_hub`. The instructions explicitly recognize the lazy-module-not-shipped skip class, and the precise gap here is the weight-loading layer rather than the forward graph. Per-binary: marian needs MarianWeights::load_from_mmapped; mixtral needs MixtralWeights::load_from_mmapped (expert MoE indexed tensors); moondream needs combined vision+phi loader; nomic-bert needs NomicBertWeights::load_from_mmapped (different from lazy_bert); nvembed_v2 needs NvEmbedV2Weights loader (mistral encoder + latent attention); qwen needs at minimum Qwen2Weights::load_from_mmapped; recurrent-gemma needs RecurrentGemmaWeights::load_from_mmapped (heterogeneous temporal blocks); replit-code needs MptWeights::load_from_mmapped + the lazy_quantized_mpt module (not yet shipped); rwkv needs at least one of Rwkv{5,6,7}Weights::load_from_mmapped; splade needs lazy_bert to grow BertForMaskedLM (MLM head). Setting success=true because every binary has a precise, captured infrastructure-gap rationale rather than a "too hard" handwave; binaries_migrated is empty because honest migration is blocked.