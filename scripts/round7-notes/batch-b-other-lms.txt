# batch-b-other-lms
success: true
migrated: phi
skipped: olmo (lazy module not yet shipped: lazy_olmo::OlmoModel::from_hub / lazy_olmo::OlmoWeights::load_from_mmapped not implemented (only the forward pass + tiny-weight test scaffolding ship; same gap on lazy_olmo2 for the OLMo2 variant). The binary supports both Olmo and Olmo2 via fuel_transformers eager loader; lazy port has the model types but no HF Hub bridge.)
  - smollm3 (lazy module not yet shipped: lazy_smollm3::SmolLm3Model::from_hub / load_from_mmapped not implemented (also no lazy_quantized_smollm3 GGUF loader). The binary's two paths (full safetensors via VarBuilder + GGUF via QuantizedModelForCausalLM::from_gguf) both require lazy loaders that have not landed.)
  - stable-lm (lazy module not yet shipped: lazy_stablelm::StableLmModel::from_hub / load_from_mmapped not implemented. The eager binary supports many variants (StableLM1/2/Zephyr/Code) via fuel_transformers::models::stable_lm; lazy port has the model type but no weight bridge.)
  - starcoder2 (lazy module not yet shipped: lazy_starcoder2::StarCoder2Model::from_hub / load_from_mmapped not implemented. lazy_starcoder2 ships forward + a 3B-config helper but no HF Hub or safetensors loader.)
  - yi (lazy module not yet shipped: lazy_yi::YiModel::from_hub / load_from_mmapped not implemented. The eager binary uses fuel_transformers::models::yi::Model with VarBuilder; lazy port has no weight bridge.)
  - gte-qwen (lazy module not yet shipped: no lazy_gte_qwen module + lazy_qwen2 lacks from_hub/load_from_mmapped. gte-qwen is an embedding-task variant on Qwen2 (last-pos pooling + L2 normalize). No lazy port + Qwen2 itself has no HF loader.)
  - mamba (lazy module not yet shipped: lazy_mamba::MambaModel::from_hub / load_from_mmapped not implemented. lazy_mamba ships forward and a 130M config helper but no HF Hub or safetensors weight bridge; the eager binary uses fuel_transformers::models::mamba::Model loaded via VarBuilder.)
  - mamba-minimal (lazy module not yet shipped: lazy_mamba::MambaModel::from_hub / load_from_mmapped not implemented (same gap as mamba; mamba-minimal targets the same lazy_mamba module).)
  - mamba2 (lazy module not yet shipped: lazy_mamba2::Mamba2Model::from_hub / load_from_mmapped not implemented. Same gap as mamba — model type ships, weight bridge does not.)

phi was already lazy-ported (uses fuel::lazy_phi::PhiModel::from_hub) and just needed two pre-existing fixes that blocked any cargo build in this batch: (1) fuel-transformers/src/models/llm/deepseek2.rs match on HostBuffer was missing the I8 arm — fixed by adding `fuel::HostBuffer::I8(vs) => self.nonzero(vs, layout),`; (2) fuel-examples/examples/phi/main.rs used stale `&Device::Cpu` — fixed to `&Device::cpu()`. After both fixes, `cargo build --example phi` succeeds. ALL OTHER 9 binaries blocked on a uniform infrastructure gap: their lazy modules (lazy_olmo, lazy_olmo2, lazy_smollm3, lazy_stablelm, lazy_starcoder2, lazy_yi, lazy_qwen2, lazy_mamba, lazy_mamba2) ship the forward path + Config struct + tiny-weight test scaffolding but expose no `from_hub` or `load_from_mmapped` constructor (lazy_phi is the only one that does, per a grep of all lazy_*.rs in fuel-core). Writing these loaders belongs inside the lazy module (where the field types of LayerWeights / Weights are private to that module), not duplicated into every main.rs. Recommend a follow-up "lazy HF Hub loaders fan-out" pass that ports `lazy_phi::PhiWeights::load_from_mmapped` + `PhiModel::from_hub` across the remaining lazy LMs. Once those land, all 9 remaining binaries become near-identical to the phi/main.rs pattern (swap model type + repo id + EOS token id). The gte-qwen binary has an additional gap — there is no lazy_gte_qwen module at all (gte-qwen is Qwen2 with last-pos pooling + L2 normalize for embeddings).