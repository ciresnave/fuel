# batch-c-vision-1
success: true
migrated: convnext
skipped: stella-en-v5 (infrastructure gap: lazy_stella_v5 has no safetensors weight loader (no StellaV5Weights::load_from_mmapped or StellaV5Model::from_hub). The lazy module is forward-only with manually-constructed Weights structs. A real-checkpoint migration requires shipping a Qwen2-base safetensors loader plus stella's embed_head loader first.)
  - t5 (infrastructure gap: lazy_t5 has no safetensors weight loader (no T5Weights::load_from_mmapped). The lazy module exposes forward()/forward_encoder()/forward_decoder() over manually-constructed T5Weights only. T5 also needs the LogitsProcessor / sampler-equivalent on the lazy side (fuel_transformers::generation::LogitsProcessor has no lazy counterpart yet).)
  - voxtral (infrastructure gap: lazy_voxtral has no safetensors weight loader. The lazy module's VoxtralEncoder/VoxtralTextModel/VoxtralModel forward methods operate on hand-constructed Weights only — no .load_from_mmapped() or .from_hub() exists. Audio mel-decoding (symphonia) path is fine to keep, but weight loading is the gate.)
  - xlm-roberta (infrastructure gap: lazy_xlm_roberta has no safetensors weight loader (no XlmrWeights::load_from_mmapped). The lazy module's XlmrModel::forward takes manually-constructed weights only.)
  - beit (infrastructure gap: lazy_beit has no safetensors weight loader (no BeitWeights::load_from_mmapped). BeitModel::forward operates on hand-constructed weights only.)
  - convmixer (infrastructure gap: lazy_convmixer has no safetensors weight loader (no ConvMixerWeights::load_from_mmapped). ConvMixerModel::forward operates on hand-constructed weights only.)
  - depth_anything_v2 (infrastructure gap: lazy_depth_anything_v2 has no safetensors weight loader. Also requires palette/enterpolation deps under the depth_anything_v2 cargo feature; even with a loader the post-processing path would need updates. Depth-Anything additionally requires a DINOv2 backbone loader which itself isn't shipped (see dinov2 skip).)
  - dinov2 (infrastructure gap: lazy_dinov2 has no safetensors weight loader (no Dinov2Weights::load_from_mmapped). Dinov2Model::forward operates on hand-constructed weights only.)
  - dinov2reg4 (infrastructure gap: lazy_dinov2reg4 has no safetensors weight loader (no Dinov2Reg4Weights::load_from_mmapped). Dinov2Reg4Model::forward operates on hand-constructed weights only.)

Batch-C-vision-1 outcome: 1 of 10 binaries migrated (convnext), 9 skipped on a single shared blocker.\n\n**Loader infrastructure is the dominant gap.** Across this batch only `fuel_core::lazy_convnext` ships a safetensors weight loader (`ConvNextWeights::load_from_mmapped` + `ConvNextModel::from_hub_with_config`). The other 9 lazy modules — stella_v5, t5, voxtral, xlm_roberta, beit, convmixer, depth_anything_v2, dinov2, dinov2reg4 — expose forward() over manually-constructed Weights structs only. There is no path from a real HF safetensors checkpoint to a lazy model without first shipping per-module loaders (each ~50-150 LOC mapping HF tensor names to the LazyTensor's Arc<[f32]>/WeightStorage weight fields).\n\n**Generic infrastructure that already exists:** `LazyTensor::from_safetensors_view`, `load_tensor_as_f32`, `load_transposed_matrix`, and `load_transposed_matrix_preserve_dtype` (the dtype-preserving BF16 path) are available on the lazy.rs side as building blocks, plus `crate::safetensors::MmapedSafetensors` for the mmap layer. So the per-module loader work is mostly mechanical name-mapping — but it's still per-module work that's out of scope for the binary-migration session.\n\n**Eager-source pre-existing breakage fixed in-line:** `fuel-transformers/src/models/llm/deepseek2.rs:50` had a non-exhaustive match on `HostBuffer` (missing `I8` arm — `HostBuffer::I8` was added to the type but never propagated into the deepseek2 NonZero impl). The whole `fuel-examples` package depends on `fuel-transformers`, so this blocked ALL example builds on this branch. Added the missing `I8(vs) => self.nonzero(vs, layout)` arm. With that one-line fix, `cargo build --example convnext` succeeds.\n\n**convnext migration shape:** eager VarBuilder → mmap MmapedSafetensors + ConvNextWeights::load_from_mmapped + ConvNextModel { config, weights }. Image still loads via fuel_examples::imagenet::load_image224 (eager Tensor) then flattens to Vec<f32> for ConvNextModel::forward's `&[f32]` input. Softmax+top-5 are done in plain Rust on the realized output since fuel_nn::ops::softmax was a fuel_transformers / fuel_nn dep we no longer want. CLI surface (Args, Which enum, all 17 variants) preserved bit-for-bit; only the `V1 small/base/large/xlarge` configs are built inline since lazy_convnext only ships the `tiny()` V1 preset (the V2 line ships all 8 presets — atto/femto/pico/nano/tiny/base/large/huge).\n\n**Recommended next session:** unblocking the 9 skips is a single "ship the safetensors loader" port per lazy module. Pattern is well-established from lazy_convnext, lazy_resnet, lazy_llama. Each loader is ~50-150 LOC + a `from_hub_with_config` wrapper. Suggest batching by HF naming family: BERT-style (xlm_roberta, beit, dinov2, dinov2reg4 — all transformer encoders), T5-encoder-decoder (t5), Qwen2-backbone (stella_v5), Whisper-Mistral hybrid (voxtral), pure CNN (convmixer), depth-head-over-DINOv2 (depth_anything_v2 — gated on dinov2 loader). Once those land, the 9 skips collapse into the same convnext-style mainrs rewrite.