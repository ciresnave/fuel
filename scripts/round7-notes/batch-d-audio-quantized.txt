# batch-d-audio-quantized
success: true
migrated: 
skipped: wuerstchen (infrastructure gap: lazy_wuerstchen.rs (1328 LOC) exposes WuerstchenConfig + 3 weight bags (PriorWeights/DiffNextWeights/PaellaVqWeights) and forward fns on PriorModel/DiffNextModel/PaellaVqModel — but no safetensors loader for any of them. The module only has `make_*_weights(cfg)` helpers that synthesize zeroed/random weights for in-tree tests. Plus there are zero `from_hub` / `MmapedSafetensors` references in the module. Migrating the binary requires building a multi-hundred-LOC safetensors→Arc<[f32]> reshuffler matching the Wuerstchen state-dict layout (decoder + prior + vqgan + 2 CLIP encoders), plus a lazy clip text encoder (the binary also uses `stable_diffusion::build_clip_transformer` which has no lazy equivalent visible). Binary also needs the lazy DDPM scheduler, ratio-broadcast embedding, etc. This is a multi-session port, not a per-binary cargo build.)
  - z_image (infrastructure gap: lazy_z_image.rs ships forward primitives but has zero `from_hub` / `MmapedSafetensors` references — no safetensors loader. Binary requires building one before migration is possible.)
  - csm (infrastructure gap: lazy_csm.rs has zero `from_hub` / `MmapedSafetensors` references — no safetensors loader exists. The CSM binary (Sesame/Conversational Speech Model) requires the loader infrastructure plus generation-loop integration with the audio decoder; the lazy module only ships the forward primitive.)
  - encodec (infrastructure gap: lazy_encodec.rs has zero `from_hub` / `MmapedSafetensors` references — no safetensors loader. Binary needs a loader for the EnCodec state-dict (encoder + decoder + residual VQ codebooks) plus encode/decode streaming integration; lazy module exposes only forward fns on pre-constructed weight bags.)
  - mimi (infrastructure gap: lazy_mimi.rs / lazy_mimi_encodec.rs / lazy_mimi_transformer.rs etc. expose forward primitives but have zero `from_hub` / `MmapedSafetensors` references — no safetensors loader. Mimi (Kyutai Mimi audio codec) requires a loader for the Mimi state-dict spanning seanet/transformer/quantization/resampler/conv sub-modules; that loader is the bulk of the migration work and has not shipped yet.)
  - metavoice (infrastructure gap: lazy_metavoice.rs / lazy_metavoice_speaker_encoder.rs have zero `from_hub` / `MmapedSafetensors` references — no safetensors loader. MetaVoice TTS binary needs loaders for the 1B base transformer + speaker encoder + tokenizer; only forward primitives exist on the lazy side.)
  - musicgen (lazy module not yet shipped: no lazy_musicgen.rs exists under fuel-core/src/. Grep of fuel-core/src/lib.rs confirms no `pub mod lazy_musicgen`. Migration blocked until the musicgen lazy port lands.)
  - parler-tts (infrastructure gap: lazy_parler_tts.rs has zero `from_hub` / `MmapedSafetensors` references — no safetensors loader. Parler-TTS needs a loader spanning the T5 encoder + audio decoder + description tokenizer; only forward primitives ship on the lazy side.)
  - snac (infrastructure gap: lazy_snac.rs has zero `from_hub` / `MmapedSafetensors` references — no safetensors loader. SNAC (multi-scale neural audio codec) binary requires a loader for the SNAC state-dict plus multi-scale residual VQ codebook reshuffling that has not been ported yet.)
  - whisper (infrastructure gap: lazy_whisper.rs DOES ship `WhisperModel::from_hub` + `forward_encoder(mel: &[f32], mel_time: usize)` + `forward_decoder(tokens: &[u32], encoder_out: &LazyTensor)`. BUT the 771-LOC eager whisper binary needs much more than the lazy forward primitives: (a) stateful encoder/decoder KV cache with `flush` semantics (no equivalent in `forward_decoder` which restarts from scratch each call); (b) `decoder_final_linear` projection separate from `forward_decoder` for sampling logits; (c) `multilingual` language-detection sub-module that runs the decoder on a probe token with SOT and extracts language logits; (d) suppress_tokens log-prob masking, temperature sampling with WeightedIndex, no_speech_prob extraction, compression-ratio rejection — all of which manipulate Tensors directly; (e) quantized Whisper variant. The lazy module exposes a clean batch-1 forward but does not yet support the incremental KV-cache + language-detection + sampling control needed by the binary. Migration would require a multi-session lazy_whisper API expansion (incremental decode + cache flush, final_linear split, multilingual hook) before the binary surface can be matched.)

Batch D (audio + quantized + multi-stage diffusion) is uniformly blocked on a shared infrastructure gap: the lazy_* modules for these models ship forward primitives + weight-bag structs + per-config `make_*_weights` test helpers, but NOT a `from_hub` / `MmapedSafetensors` loader that turns a published safetensors checkpoint into the weight bag.

Evidence: across lazy_wuerstchen, lazy_z_image, lazy_csm, lazy_encodec, lazy_mimi (+ all 8 mimi sub-modules), lazy_metavoice, lazy_metavoice_speaker_encoder, lazy_parler_tts, lazy_snac — the count of `from_hub`/`MmapedSafetensors`/`safetensors::` references is exactly 0. Only lazy_whisper.rs has these (9 references) and ships `WhisperModel::from_hub`.

Whisper has the loader but the eager binary requires API surface the lazy module doesn't expose yet: incremental KV cache with flush semantics, separate `decoder_final_linear` projection, language-detection hook, suppress_tokens masking, temperature/no-speech-prob/compression-ratio sampling controls, and a quantized model variant. The lazy `forward_decoder(&[u32], &encoder_out)` is a single-shot batch-1 primitive; it does not yet support the streaming/decoding loop the binary embodies.

musicgen has no lazy_musicgen.rs at all — the lazy port has not landed.

Each individual binary is a multi-session port (loader + extended forward API + sampling control), not a per-binary cargo build cycle. Returning success=true with all 10 skipped under specific infrastructure-gap reasons so the parent agent can route the loader+sampling-API work as its own gate before re-attempting binary migration.

Recommendation: a "phase G+" lazy-side session shipping `from_hub` + safetensors-shaped loaders for these 8 audio/quantized modules, plus a lazy-Whisper API expansion (incremental KV cache, multilingual hook, sampling control), would unblock this entire batch.